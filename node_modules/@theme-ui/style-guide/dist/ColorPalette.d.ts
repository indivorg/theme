/// <reference types="react" />
import { ColorSwatchProps } from './ColorSwatch';
declare type Colors = Record<string, string | {
    [key: string]: Colors;
}>;
export interface ColorRowProps extends Omit<ColorSwatchProps, 'color'> {
    colors: Colors;
    name?: string;
    omit?: string[];
    render?: (value: {
        swatch: React.ReactElement;
        color: string;
        key: string;
        name: string;
    }) => React.ReactNode;
    size?: number | string;
}
export declare const ColorRow: ({ colors, name, omit, render, size, ...props }: ColorRowProps) => JSX.Element;
export interface ColorPaletteProps extends Omit<ColorRowProps, 'colors'> {
    omit?: string[];
    mode?: string;
}
export declare const ColorPalette: ({ omit, mode, ...props }: ColorPaletteProps) => JSX.Element;
export default ColorPalette;
